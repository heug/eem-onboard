version: 2

jobs:
  
  build:
    docker:
      - image: circleci/node:6
    steps:
      - checkout
      - run: echo "hello!"

  test:
    docker:
      - image: circleci/node:6
    steps:
      - run: echo "goodbye!"

#   download_artifacts:
#     docker:
#       - image: circleci/ruby:2.8
#     steps:
#       - run: echo "downloaded artifacts"

workflows:
  version: 2
  build_and_test:
    - build
    - test
